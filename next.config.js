/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  images: { unoptimized: true },

  basePath: "https://sinaanvarinia.github.io/tavangaran-sahand/",
};

module.exports = nextConfig;
